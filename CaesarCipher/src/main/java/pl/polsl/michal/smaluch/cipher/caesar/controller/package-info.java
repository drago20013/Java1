/**
 * The package contains two classes: {@link pl.polsl.michal.smaluch.cipher.caesar.controller.CipherAppController} and {@link pl.polsl.michal.smaluch.cipher.caesar.controller.CipherAppRunner} needed for controlling the program.
 * 
 * Controller handles interaction between Model and View, manipulates Model and notifies View of the change.
 * It depends on both the model and the view packages.
 */
package pl.polsl.michal.smaluch.cipher.caesar.controller;
